# P2.8-B.1 — Cardano-ledger runner handoff — 2026-09-23

## Verified current state

The operational Green Closure branch contains a fail-closed runner at `audit/cardano-ledger-runner/Main.hs`. It verifies the presence and non-empty status of:

- tx.cbor
- utxo.json
- pparams.json
- epoch-info.json
- system-start.json
- manifest.json

It deliberately stops before evaluation and prints a safe-stall / implementation-next message.

The runner project is already pinned to specific Cardano Ledger and Plutus source commits in `cabal.project`. The current cabal dependencies include cardano-ledger-alonzo, cardano-ledger-core, plutus-core and plutus-ledger-api.

## Concrete implementation target

The missing bridge is:

evidence packet -> typed Tx / UTxO / PParams / EpochInfo / SystemStart -> ledger-aligned `evalTxExUnitsWithLogs` -> persisted ExUnits or TransactionScriptFailure + logs.

Do not substitute a synthetic transaction, synthetic UTxO, guessed cost model, or emulator result for the exact packet.

## Important ledger-context observation

Cardano Ledger exposes UTxO lookup and script-provisioning paths from the typed UTxO context; starting with Babbage, reference inputs can provide scripts from the UTxO. This reinforces that the evaluator must reconstruct the actual ledger context rather than only decode declared JSON fields.

## Current classification

P2.8-B.1 = BLOCKED / CONCRETE IMPLEMENTATION TARGET IDENTIFIED.

Green requires an actual typed ledger evaluation with persisted measurable result or exact ledger-originated TransactionScriptFailure.

No economic constants, validator semantics, datum/redeemer semantics, or protocol rules changed.

## RT-1.5 / C15 companion note

The FundTreasury real-Yaci trace includes the duplicate signed-transaction replay witness. Fresh CI after that witness is still unverified; wrong/stale Oracle, wrong Pool, double-count and valuation-mismatch ledger twins remain open.

## Prior-art / CAES coordination

The useful reusable test pattern remains transition-identity continuity across proposal, admission, reconstruction, revalidation and observation. This is a test/evidence hardening pattern, not a new economic rule or novelty claim.
