# P2.8 Cardano-ledger Evaluation Dossier

**Status:** Infrastructure path repaired and evidence workflow ready; native evaluator result remains pending a fresh successful build/evaluation run.

## Runner contract

The P2.8 runner is fail-closed. It requires:
- exact Plutus validator artifacts,
- transaction CBOR,
- consumed UTxO packet,
- era-specific protocol parameters,
- typed timing context,
- SHA-256 manifest binding.

Missing or synthetic context causes SAFE_STALL rather than substitution.

## Dependency repair sequence

The runner crossed a sequence of concrete source-level dependency blockers:
- Conway package path added to the pinned ledger source.
- cardano-base aligned to a compatible release.
- cardano-binary pinned separately to the required release.
- cardano-crypto-praos pinned to the required release.
- cardano-crypto-leios pinned to the required release.
- cardano-crypto aligned to the ledger dependency surface.
- cardano-crypto-class corrected away from the incompatible 2.6.x source pin.
- Byron executable-spec dependencies added after the resolver exposed `byron-spec-ledger` as a missing package.
- Ogmios-native timing packet decoding added alongside legacy Yaci decoding.

The latest observed resolver failure before the Byron correction was:

`unknown package: byron-spec-ledger (dependency of cardano-ledger-byron)`

The repository now includes `eras/byron/chain/executable-spec` and `eras/byron/ledger/executable-spec` in the pinned ledger source package.

## Current verification

The latest observed P2.8 run is still executing/has not yet produced a successful native evaluation result. It therefore remains **OPEN**.

## Preprod materialization path

New workflow:

`.github/workflows/immortal-preprod-ledger-evidence.yml`

New materializer:

`audit/cardano-integration/materialize-preprod-p28-context.mjs`

The materializer is deliberately non-signing. It accepts only an already-submitted transaction hash and obtains:
- transaction CBOR and transaction metadata from Koios Preprod,
- exact consumed UTxOs,
- protocol parameters for the transaction's epoch,
- era/epoch timing from Demeter/Ogmios,
- SystemStart from Demeter/Ogmios.

It rejects a transaction unless its epoch is identified as **Babbage**, because the current P2.8 runner is Babbage-typed.

## Important limitation

The typed `EpochInfo` constructor is derived from the exact era parameters returned by Ogmios for the transaction epoch. This is deterministic typed-context construction; it is not a claim that the runner has reconstructed a complete historical consensus state beyond the inputs required by its current evaluation surface.

## Acceptance criteria

A complete P2.8 packet becomes VERIFIED only when the fresh workflow records:
- native Babbage transaction decode,
- native UTxO decode,
- native PParams decode,
- typed EpochInfo decode,
- typed SystemStart decode,
- `evalTxExUnitsWithLogs` completion,
- explicit A/B or typed-failure result,
- binding hashes for all source/context/evaluation artifacts.
